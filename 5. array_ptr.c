#include <stdio.h>

int main() {

    int values[3] = {100, 200, 300};

        printf("First element (index 0): %d\n", *values);

        printf("Second element (index 1): %d\n", *(values + 1));

              return 0;
}